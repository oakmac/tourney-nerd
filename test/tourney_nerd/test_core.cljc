(ns tourney-nerd.test-core
  (:require
    [clojure.test :refer :all]))
    ; [tourney-nerd.foo :refer [banana]]))

(deftest example-test-aaa
  (is (= 1 1)))

; (deftest example-test-bbb
;   (testing "Banana Test"
;     (is (= "banana" (banana)))))
